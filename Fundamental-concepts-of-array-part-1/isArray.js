const names = ["Alam", "Balam", "Kalam", "Salam"];
const num = [10, 20, 30, 40];
const ages = [];
const food = 'ros O go ll a';
const randomNum = 15;

console.log(Array.isArray(names));
console.log(Array.isArray(num));
console.log(Array.isArray(ages));
console.log(Array.isArray(food));
console.log(Array.isArray(randomNum));

/**
 * More about array func:
 * 
 * 1. join()
 * 2. concate()
 * 3. slice()
 * 4. splice()
 */
