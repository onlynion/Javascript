// const person = ["rakib", "nokib", "sakib", "dakib", "akib"];
// const sortedPerson = person.sort();
// console.log(sortedPerson);

/**
 * sort
 * ascending ---> smaller to larger
 * descending ---> larger to smaller
 */

const number = [4, 7, 2, 8, 3, 6];
const sort_number = number.sort();
console.log(sort_number);
